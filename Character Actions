class Captain(Character):
    def action(self, target):
        # Captain can imprison a player, preventing their action for that night
        target.imprisoned = True

class Lookout(Character):
    def action(self, target):
        # Lookout can observe a player to see if they perform any night actions
        return target.has_night_action

# Similar implementations for other characters...
